_generated: honey checksum:19bf4aa16f1e
info:
  description: null
  title: Honey CF
  version: "0.1.0"
openapi: "3.1.0"
paths:
  /product/{id}:
    get:
      requestBody:
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: "#/components/schemas/GetProductRequest"
        required: true
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetProductResponse200"
          description: ok
components:
  schemas:
    GetProductRequest:
      type: string
      format: binary
      contentEncoding: binary
    GetProductResponse200:
      type: object
      properties:
        name:
          type: string
      required:
        - name
      additionalProperties: false
