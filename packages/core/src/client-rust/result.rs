/* Code generated by honey. DO NOT EDIT. */

use crate::errors::Error;

/// SdkResult is a generic result container for safe-mode SDK calls (throwOnError=false).
/// In safe mode, methods return SdkResult<T> instead of T.
/// The error is embedded in the result — the outer Result retains only transport/parse errors.
/// E defaults to Error (generic transport/api error) but per-op typed error envelopes
/// can substitute a narrower type (e.g. SdkResult<User, UsersCreateError>).
pub struct SdkResult<T, E = Error> {
    pub data: Option<T>,
    pub error: Option<E>,
    pub status: u16,
    pub response: crate::runtime::ResponseMeta,
}

impl<T, E> SdkResult<T, E> {
    /// ok returns true when the result carries no error.
    pub fn ok(&self) -> bool {
        self.error.is_none()
    }
}
